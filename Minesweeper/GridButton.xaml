<UserControl x:Class="Minesweeper.GridButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Name="Control"
             d:DesignHeight="300" d:DesignWidth="300">
    <ToggleButton Margin="0" Padding="0"  Name="Button" 
                  DataContext="{Binding ElementName=Control}" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  Background="{Binding ButtonColor, FallbackValue=Pink}" 
                  IsChecked="{Binding Clicked, Mode=OneWay}" 
                  PreviewMouseLeftButtonDown="OnLeftClick" 
                  MouseRightButtonUp="OnRightClick">
        <Label Margin="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <Viewbox>
                <TextBlock Foreground="Black" Text="{Binding ButtonText, FallbackValue='-'}"/>
            </Viewbox>
            
        </Label>
    </ToggleButton>
</UserControl>
